#include <iostream>
#include <string>
using std::cin;
using std::string;

int main(int argc, char const *argv[]) {
  string line;
  getline(cin, line);  // 读入一整行
  string word;
  cin >> word;  // 读入一个词
  return 0;
}
