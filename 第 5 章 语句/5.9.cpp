#include <iostream>
using std::cin;

int main(int argc, char const *argv[]) {
  unsigned aCnt = 0, eCnt = 0, iCnt = 0, oCnt = 0, uCnt = 0;
  char ch;
  while (cin >> ch) {
    if (ch == 'a') {
      ++aCnt;
    } else if (ch == 'e') {
      ++eCnt;
    } else if (ch == 'i') {
      ++iCnt;
    } else if (cn == 'o') {
      ++oCnt;
    } else if (cn == 'u') {
      ++uCnt;
    }
  }

  return 0;
}
